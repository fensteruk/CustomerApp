<x-layouts.portal :title="($target->is_active ? 'Deactivate ' : 'Reactivate ').$target->name.' | Fenster'" sidebar-label="Menu">
    <div class="admin-workspace max-w-3xl"><a class="admin-back" href="{{ route('office.workspace.users.show', $target) }}">← {{ $target->name }}</a><header><p class="eyebrow">User status</p><h1 class="admin-title">{{ $target->is_active ? 'Deactivate User' : 'Reactivate User' }}</h1></header><div class="admin-notice"><p>{{ $target->is_active ? 'This user will be unable to access the portal. Assignments and history will be retained.' : 'This user can sign in again, but access still depends on an active customer and active assigned sites.' }}</p></div><form class="admin-card" method="POST" action="{{ route('portal.office.users.'.($target->is_active ? 'deactivate' : 'reactivate'), $target) }}">@csrf<input type="hidden" name="lock_version" value="{{ $target->lock_version }}"><div class="admin-actions"><button class="{{ $target->is_active ? 'reject-button' : 'primary-button' }}" type="submit">Confirm {{ $target->is_active ? 'Deactivation' : 'Reactivation' }}</button><a class="secondary-button" href="{{ route('office.workspace.users.show', $target) }}">Cancel</a></div></form></div>
</x-layouts.portal>
